#include <iostream>

// TODO: Define a global constant here

int main() {
    // TODO: Implement the following:
    // 1. Create a variable in the main scope
    // 2. Create a nested block with its own variable
    // 3. Create another nested block that:
    //    - Uses the global constant
    //    - Uses the main scope variable
    //    - Defines its own variable
    // 4. Demonstrate variable visibility at each scope level
    
    return 0;
}
